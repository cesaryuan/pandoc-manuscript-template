[project]
name = "pandoc-manuscript-template"
version = "0.1.1"
description = "Pandoc Markdown manuscript template with DOCX-focused post-processing"
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [
  { name = "cesaryuan" },
]
keywords = ["pandoc", "docx", "manuscript", "markdown", "academic-writing"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Topic :: Scientific/Engineering",
  "Topic :: Text Processing :: Markup :: Markdown",
]
dependencies = [
  "python-docx>=1.1.0",
  "pyyaml>=6.0",
  "lxml>=4.9.0",
  "panflute>=2.3.0",
  "pymupdf>=1.26.0",
  "pydantic-settings>=2.0.0",
  "python-dotenv>=1.0.0",
  "py7zr>=0.22.0",
  "resvg-py>=0.3.3",
]

[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E9"]

[tool.bumpversion]
current_version = "0.1.1"
parse = "(?P<major>\\d+)\\.(?P<minor>\\d+)\\.(?P<patch>\\d+)"
serialize = ["{major}.{minor}.{patch}"]
commit = true
tag = true
tag_name = "v{new_version}"
message = "chore: release {new_version}"
tag_message = "Release {new_version}"
allow_dirty = false

[project.scripts]
pmt = "pandoc_manuscript.cli:main"
pandoc-manuscript = "pandoc_manuscript.cli:main"

[project.urls]
Homepage = "https://github.com/cesaryuan/pandoc-manuscript-template"
Repository = "https://github.com/cesaryuan/pandoc-manuscript-template"
Issues = "https://github.com/cesaryuan/pandoc-manuscript-template/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
exclude = [
  ".git",
  ".pmt",
  ".pandoc-cache",
  ".vscode",
  "output",
  "tmp",
  "**/__pycache__",
  "**/*.pyc",
  "scripts/mathtype-rust/target",
]

[tool.hatch.build.targets.wheel]
packages = ["src/pandoc_manuscript"]

[tool.hatch.build.targets.wheel.force-include]
"pandoc" = "pandoc_manuscript/pandoc"
"template" = "pandoc_manuscript/_template"
