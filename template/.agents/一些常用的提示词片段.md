这个文件中放那些不适合放在 AGENTS.md 中或者制作成 SKILL 的内容，主要是一些常用的提示词片段，方便你在写作中直接使用。

## 审稿意见返回后修改

请你根据整篇论文的内容 manuscript.md 以及 comments_first_zh.md 审稿人的意见，为每个意见撰写一个对应的修改意见放到一个单独的 md 文件中，并分析意见的修改难度。每条意见包含几个部分：审稿意见原文、审稿意见原文翻译（和前者不空空行）、建议修改方向、修改难度

我对你提供的建议修改思路进行了审核，对于确定了的修改方向记上了（已人工确定）标记，但是还有一些我没有确定下来的修改方向需要和你讨论一下，我已经在文件中用 `< !-- todo:`  进行了标记，请你阅读并回复我

自提交 03764d7a852439f09df27a3fa9c33aa8f33ce4fd1781090032750 以来的 manuscript.md 的修改的内容中新增的句子大量使用了 This 等我觉的不太符合 SCI 论文写作风格的句子（不过只是我觉得，你要自己思考一下），请你检查一下自那以来的新增句子的语法和风格，润色一下，使其更符合 sci 论文，包括但不限于检查以下内容：
- 检查新增的句子中有没有审稿回复痕迹，就是那种明显是为了回复审稿人的语气的句子
- 检查新增的句子中有没有太过自限性的话语，容易让审稿人抓住把柄

检查一下自提交 03764d7a852439f09df27a3fa9c33aa8f33ce4fd1781090032750 以来的 manuscript.md 的修改或新增的主要内容有哪些并没有在 submissions\dbe\reply_to_reviewers_draft.md 被提到（仅需要检查较大的更改，对于只修改了一两个单词的修改不需要检查）

检查所有的参考文献 bib，按照 DOI 检索元数据，看看有没有错误的

自 6bca278b740410da248869bfd846b2e6f7bd29e3 提交以来论文被老师进行了比较大的更改，但是老师修改的比较急，请你检查一下修改的质量，有没有对论文上下文一致性、逻辑连贯性造成影响

## 修改部分标红

自 77ab513da20bfa1816a25e5c5ac32925c4549dbb 提交以来我的论文按照审稿人的审稿意见修改了很多，期刊要求生成一份修改部分用红色标注出来的 Word 文档，你帮我修改 manuscript.md，将其中修改或新增的部分标红，标红方式使用 pandoc 的 custom styles 功能（[修改的部分]{custom-style="Revision Char"}, blabla.），有以下几个点要注意：  
1. 对于一句话中只小于三个单词的修改，不需要标红，直接展示原文即可
2. 对于删除原来的话替换为新的话的地方，原来的话不需要用删除线标记出来，只用红色把修改的或新增的段落或者句子标红即可 
3. 对于修改或新增的图片（只有图片内容修改算作修改，图片大小修改不算），把该图片对应的题注标红 
4. 对于新增的表格，直接添加 attribute revision-rows="*"。对于修改的表格，如果添加或修改了某列，则添加 revision-columns="col1num,col2num" 如果添加或者修改了某行，则添加 revision-rows="row1num,row2num" 属性
5. 对与那些进行了比较大改动的段落，用 Revision Char 务必**仅将改动部分标记出来**比较好。当且仅当整个段落全部都是新增的，之前完全没有这个段落的时候才将整个段落用 Revision Char 标记出来

Custom Styles 的文档（文档中已经有了名为 Revision Char 的字符样式可供使用）：
Custom StylesCustom Styles 
Custom styles can be used in the docx, odt and ICML formats.

Output
By default, pandoc’s odt, docx and ICML output applies a predefined set of styles for blocks such as paragraphs and block quotes, and uses largely default formatting (italics, bold) for inlines. This will work for most purposes, especially alongside a reference doc file. However, if you need to apply your own styles to blocks, or match a preexisting set of styles, pandoc allows you to define custom styles for blocks and text using divs and spans, respectively.

If you define a Div, Span, or Table with the attribute custom-style, pandoc will apply your specified style to the contained elements (with the exception of elements whose function depends on a style, like headings, code blocks, block quotes, or links). So, for example, using the bracketed_spans syntax,

[Get out]{custom-style="Emphatically"}, he said.
would produce a file with “Get out” styled with character style Emphatically. Similarly, using the fenced_divs syntax,

## 提交前检查

- 检查是否存在不必要的文献引用或者错误的引用、
- 核查全文的表格数据，重点检查：
  1. 前后有没有不一致
  2. 数值明显不合理
- 低级错误，如有错别字、拼写错误、语法错误等
- 检查回复信中对原文的引用是否和正文完全一致（换行也要一致）
- 在学术期刊中，表格和图片的编号必须严格按照在正文中首次出现的顺序排列。检查是否有不符合要求的地方